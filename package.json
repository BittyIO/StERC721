{
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "@openzeppelin/contracts-upgradeable": "^5.3.0"
  },
  "scripts": {
    "test": "forge test -vvv",
    "coverage": "forge coverage --no-match-coverage 'test|node_modules' --report lcov && genhtml lcov.info --branch-coverage --output-dir coverage --ignore-errors inconsistent"
  }
}
