{
  "dependencies": {
    "@openzeppelin/contracts": "^5.3.0",
    "@openzeppelin/contracts-upgradeable": "^5.3.0",
    "@openzeppelin/foundry-upgrades": "^0.4.0"
  },
  "scripts": {
    "test": "forge test -vvv",
    "coverage": "forge coverage --ir-minimum --no-match-coverage 'test|node_modules' --report lcov && genhtml lcov.info --branch-coverage --output-dir coverage --ignore-errors inconsistent",
    "deploy:sepolia": "source .env && forge script script/deploy.sol:DeployScript --chain sepolia --rpc-url $SEPOLIA_RPC_URL --slow --broadcast --verify -vvv --interactives 1 && npx tsx script/deplyment.ts --network sepolia"
  }
}
